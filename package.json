{
  "name": "lolisafe-extension",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint",
    "build": "web-ext build -a ./web-ext-artifacts -i README.md sign.sh web-ext-artifacts --overwrite-dest",
    "sign": "web-ext sign -a ./web-ext-artifacts-i README.md sign.sh web-ext-artifacts",
    "debug": "web-ext run --verbose -i README.md sign.sh web-ext-artifacts"
  }
}
